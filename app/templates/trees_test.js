'use strict';

describe('myApp.trees module', function() {

  beforeEach(module('myApp.trees'));

  describe('trees controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
